{
  "name": "searchy",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/style.css -o ./dist/style.css && vite --host",
    "build": "tsc && npx tailwindcss -i ./src/style.css -o ./dist/style.css && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "daisyui": "^3.9.4",
    "tailwindcss": "^3.3.5",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
